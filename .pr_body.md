## 요약
전문가 평가에서 지적된 보안/UX/운영 미흡 사항 중 코드 변경만으로 즉시 막을 수 있는
P0-P2 항목을 조치한다. 인프라 의존(CAPTCHA/이메일 인증/Redis/MFA 등)은
`docs/HARDENING_BACKLOG.md`에 백로그로 남기고 코드 주석으로 가리켰다.

## 변경 사항
### P0 — 즉시 반영
1. **회원가입 IP 레이트리밋** (`signup_rate_per_ip_per_min=5`/분, 기본값)
   - 봇 대량 계정 생성 1차 방어. 다중 워커/Redis는 백로그.
2. **비밀번호 재설정 IP 레이트리밋** (`password_reset_rate_per_ip_per_min=5`/분)
   - 메일 폭탄·계정 존재 열거 속도 제한.
3. **AI 안전 가드레일** (`app/services/context.py`)
   - 시스템 프롬프트에 불법/유해 요청 거절, 의료·법률·재정 사칭 금지,
     시스템 프롬프트 유출 시도 거절, 모르면 모른다고 답변 규칙 추가.
   - 프롬프트 인젝션 패턴 탐지 시 숨겨진 경고문을 자동 삽입(사용자에게는 노출 안 됨,
     원문 DB 저장 유지).

### P1 — 안전/가용성
4. **`/readyz` 헬스체크** 신설
   - DB `SELECT 1` 성공 시 200, 실패 시 503. readiness probe 용도.
   - 기존 `/health`는 외부 의존성 없는 라이트 버전 그대로 유지(liveness용).
5. **429 Retry-After 한국어 친화 메시지**
   - 60초 미만은 "약 N초 후", 이상은 "약 M분 S초 후" 안내를 모든 429 응답에 포함.
6. **비밀번호 블랙리스트**
   - 흔한 비밀번호 40여 개 + 동일 문자 반복 비밀번호 거부
     (대소문자/구두점 정규화 포함). 회원가입과 비밀번호 재설정 모두에 적용.
7. **이메일 로컬파트 길이 제한** (64자, RFC 5321)
   - EmailStr만으로는 막지 못하는 극단적인 입력 차단.
8. **요청 바디 크기 미들웨어** (`max_request_body_bytes`, 기본 1 MiB)
   - Content-Length로 먼저 차단. 메모리 DoS 방어. 엣지/로드밸런서 추가 차단은 백로그.

### P2 — 운영/정리
9. 429 이벤트 로깅 허용 목록 2건 추가 (`signup_rate_limited`,
   `auth_password_reset_ip_rate_limited`).
10. 인프라 의존 백로그 문서 `docs/HARDENING_BACKLOG.md` 작성, 코드 TODO 주석 연결.

## 테스트
- 신규 단위 테스트 21건 추가(비밀번호 블랙리스트, Retry-After 포매터, /readyz).
- 기존 테스트 정리: 레이트리밋 오염 방지 픽스쳐가 신규 2개 limiter도 초기화하도록 보강,
  블랙리스트에 걸리지 않는 예시 비밀번호로 교체.
- `pytest`: **249 passed**
- `ruff check`, `black --check`, `isort --check` all pass.

## 백로그 (별도 이슈/마일스톤)
인프라가 필요해 이 PR에서 다루지 않은 항목은 `docs/HARDENING_BACKLOG.md` 참고:
- CAPTCHA/Turnstile, 이메일 인증, Redis 분산 리밋, Structured JSON 로깅,
  엣지 레벨 바디 사이즈 제한, MFA, 비밀번호 이력 제한, 자동 취약점 스캔.

## 배포 영향
- Config 3개 신규 필드(기본값 있음): `signup_rate_per_ip_per_min`,
  `password_reset_rate_per_ip_per_min`, `max_request_body_bytes`.
- 신규 엔드포인트 `/readyz`, 기존 `/health` 동작 유지.
- DB 마이그레이션 불필요.
