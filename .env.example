# ─────────────────────────────────────────────────────────────
# 환경변수 예시 — 실제 값은 각자의 .env에만 작성 (.env는 .gitignore됨)
# 복사 후: cp .env.example .env
# ─────────────────────────────────────────────────────────────

# [필수] AI 제공사 API 키. 없으면 데모 모드로 동작
AI_API_KEY=

# ── 코디세이 '네이토' (OpenAI 호환) 사용 예 ──────────────────
# 아래 3줄만 바꾸면 코드 수정 없이 연결됩니다.
# 실제 엔드포인트/모델명은 코디세이 콘솔의 API 문서에서 확인:
#   AI_BASE_URL=https://(코디세이-호스트)/v1        ← /v1까지만 적어도 됨(/chat/completions 자동 추가)
#   AI_MODEL=(네이토 모델명)
#   AI_API_KEY=(코디세이 발급 키)
# 연결 확인: python scripts/ai_check.py
# ─────────────────────────────────────────────────────────────

# AI 엔드포인트/모델 (OpenAI 호환: OpenAI / Groq / Novita / 코디세이 네이토 등)
AI_BASE_URL=https://api.openai.com/v1/chat/completions
AI_MODEL=gpt-4o-mini

# 운영 안정성 — 타임아웃(초)과 재시도 횟수
AI_TIMEOUT_SEC=10
AI_MAX_RETRIES=1

# 컨텍스트: 직전 N개의 Q/A를 AI 프롬프트에 포함
CONTEXT_TURNS=5

# DB (SQLite 권장)
DATABASE_URL=sqlite:///./app.db

# 세션 쿠키 서명 키 — 운영에서는 반드시 긴 무작위 값으로 변경
# 생성 예시: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change-me-to-random-string
