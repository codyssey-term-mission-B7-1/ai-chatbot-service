# 역할별 TODO 리스트 — 작업 시작 가이드

> 대응 이슈: `#1 ~ #13` (역할 카드) · 각 카드 3개씩 선택해 담당
> 브랜치: `feature/#이슈번호-설명` → PR (리뷰어 1명) → develop 머지
> 커밋: `type(scope): 제목 (#이슈번호)` · 하루 2~3개로 쪼개 커밋

## 조합 추천 (A~D)

| 조합 | 카드 | 첫 주 집중 |
|------|------|-----------|
| 🔧 A 서버 코어 | 3+4+5 (+1 PM 겸직) | D2~D5 |
| 🤖 B AI·데이터 | 6+7+8 | D4~D7 |
| 🎨 C 프론트·형상 | 9+10+2 | D3~D8 |
| 🛠 D 운영·품질·문서 | 11+12+13 | D4, D5(배포), D8~D12 |

---

## 🔧 조합 A — 서버 코어

### [카드 03] FastAPI 서버 설계 → 이슈 #3
- [ ] 로컬 환경 세팅: `git clone` → venv → `pip install -r requirements-dev.txt` → 서버 기동 확인
- [ ] `app/main.py` 라우팅/예외 핸들러 리뷰하고 구조 이해 (스켈레톤 구조 주석 보강)
- [ ] `/health` 응답에 버전·모드 정보 추가
- [ ] 전역 예외 핸들러에 로그 이벤트(`unhandled_error`) 정리
- [ ] README 시스템 구조 섹션에 컴포넌트 설명 보강
- 완료 조건: 구조 문서화 + 모든 라우트에 summary/description 부여 (Swagger 품질)

### [카드 04] 회원 인증 → 이슈 #4
- [ ] 회원가입/로그인/로그아웃 흐름 코드 리뷰 (`app/routers/auth.py`)
- [ ] 비밀번호 정책 강화 옵션 조사 (숫자/특수문자 조합 — 선택)
- [ ] 로그인 시 `user_login` 로그에 이메일 도메인 외 정보 누락 확인(개인정보 최소화)
- [ ] 통합 테스트 `test_auth_flow.py` 케이스 추가 (예: 닉네임 기본값)
- 완료 조건: 새 테스트 1개 이상 추가 및 통과

### [카드 05] 접근 제어 → 이슈 #5
- [ ] `get_current_user` 가드 적용 범위 점검 (모든 보호 라우트)
- [ ] 비로그인 UI 분기: 채팅 페이지 진입 시 로그인 리다이렉트 동작 확인
- [ ] 세션 만료 시나리오 테스트 (임의 만료 → 401)
- [ ] 다른 사용자 로그 접근 차단 테스트 유지
- 완료 조건: 접근 제어 매트릭스 문서 작성 (URL × 로그인/비로그인)

---

## 🤖 조합 B — AI 파이프라인 & 데이터

### [카드 06] AI API 연동 → 이슈 #6
- [ ] 코디세이 '네이토' 실제 엔드포인트/모델명 확인 → `.env` 반영
- [ ] `python scripts/ai_check.py`로 실제 키 연결 검증
- [ ] 인증 헤더/응답 형식이 표준과 다르면 `app/services/ai_client.py` 조정
- [ ] `AI_TIMEOUT_SEC`/`AI_MAX_RETRIES` 값 튜닝 (실측 latency 참고)
- [ ] 타임아웃 강제 유발(`AI_TIMEOUT_SEC=0.001`) → 504 스크린샷 증빙
- 완료 조건: 실제 API로 문맥 응답 성공 + 타임아웃 증빙 캡처

### [카드 07] 컨텍스트 & 챗 파이프라인 → 이슈 #7
- [ ] `CONTEXT_TURNS` 값 실험 (3/5/10 — 토큰 비용 vs 문맥 품질)
- [ ] 시스템 프롬프트(`app/services/context.py`) 다듬기 — 문맥 인용 안내 강화
- [ ] 긴 대화에서 컨텍스트 초과 시 동작 확인 (N개 초과 시 오래된 것 제거)
- [ ] "내가 방금 뭘 물어봤지?" 데모 시나리오 완성도 점검
- 완료 조건: 연속 3질문 이상 문맥 유지 시연 성공

### [카드 08] DB 설계 & 로그 → 이슈 #8
- [ ] ERD 이미지 생성 후 README에 삽입 (mermaid or draw.io)
- [ ] `scripts/check_logs.sql` 결과 캡처 (평가 증빙용)
- [ ] 인덱스 점검: `chat_logs(user_id)`, `chat_logs(created_at)`
- [ ] 대화 로그에 모델명 필드 추가 여부 검토 (선택 — 어느 모델이 답했는지 추적)
- 완료 조건: ERD 게시 + 로그 조회 증빙

---

## 🎨 조합 C — 프론트엔드 & 형상관리

### [카드 09] 채팅 UI → 이슈 #9
- [ ] 마크다운/줄바꿈 렌더링 개선 (AI 응답 가독성)
- [ ] Enter 전송 / Shift+Enter 줄바꿈 처리
- [ ] 채팅 중 이중 전송 방지 (이미 처리됨 — 확인 및 테스트)
- [ ] 반응형 레이아웃 점검 (모바일)
- [ ] UI 수동 체크리스트 작성 (빈 입력/1000자/로딩/에러 표시 — 스크린샷 포함)
- 완료 조건: 체크리스트 통과 + 스크린샷 세트

### [카드 10] 인증·기록 UI → 이슈 #10
- [ ] 회원가입 폼 실시간 검증 메시지 (이메일 형식/비번 길이)
- [ ] 로그인 에러 메시지 표시 확인 및 개선
- [ ] 내 기록 화면: 질문 클릭 시 전체 답변 펼침 (선택)
- [ ] 가입→로그인→기록 조회 흐름 스크린샷 세트 (문서 증빙용)
- 완료 조건: 화면 흐름 스크린샷 완성

### [카드 02] Git / 형상관리 → 이슈 #2
- [ ] 팀원 온보딩: clone/브랜치/PR 흐름 공유 (10분 미팅)
- [ ] 매주 금요일 팀원별 커밋/PR 수 점검 (인당 커밋 2~3/주 목표)
- [ ] `.env` 커밋 방지 확인 (레포에 없는지 전수 점검)
- [ ] PR 라벨(`be`/`fe` 등) 정착 및 프로젝트 보드 연결 상태 점검
- 완료 조건: D12에 팀원 전원 커밋 10회+ 달성

---

## 🛠 조합 D — 운영 & 품질 & 문서

### [카드 11] 로깅 & 입력 검증 → 이슈 #11
- [ ] 6종 로그 이벤트 실서버 캡처 (증빙 자료로 docs/에 추가)
- [ ] 로그에 API 키/질문 전문(50자+) 미노출 검사 코드/리뷰 규칙화
- [ ] 요청별 `request_id`로 파이프라인 추적하는 방법 문서화
- [ ] 회원가입 검증 케이스 추가 (잘못된 이메일 형식 등)
- 완료 조건: 로그 증빙 패키지 + 검증 테스트 통과

### [카드 12] 배포 & 운영 → 이슈 #12
- [ ] 배포 플랫폼 선정/계정 생성 (D1)
- [ ] **뼈대 1차 배포** (D5) — `uvicorn app.main:app --host 0.0.0.0`
- [ ] 저장소 Secrets에 `DEPLOY_WEBHOOK_URL`/`DEPLOY_URL` 등록 → CD 워크플로 활성화
- [ ] 배포 후 매일 `scripts/e2e_smoke.sh` 통과 확인
- [ ] README 배포 섹션 완성 (플랫폼별 스크린샷)
- 완료 조건: 외부 URL에서 스모크 통과 + CD 파이프라인 동작

### [카드 13] 기술 문서 & 검증 패키지 → 이슈 #13
- [ ] 각 파트 초안 취합 (D10) — 팀원 제출 → 통합 편집
- [ ] README에 팀 구성/개인별 작업 요약 작성 (Git 이력과 일치하도록)
- [ ] `docs/API.md` 최신화 (변경된 API 반영)
- [ ] 평가 대비 증빙 목록化管理: 타임아웃 캡처/로그 캡처/스모크 로그/E2E 결과
- [ ] 최종 요구사항 체크리스트 대조 (D12)
- 완료 조건: 제출 패키지 완성 (M3)

---

## 공통 (전원)

- [ ] 매 작업일 스탠드업 참석 (10분)
- [ ] 리뷰 요청된 PR은 다음 작업일 내 처리
- [ ] 막히면 `blocked` 라벨 + 스탠드업 공유
