# 07. 트러블슈팅 기록 (Troubleshooting Log)

> **문서 이력** | 2026-08-30 ~ 2026-09-14 발생 이슈를 정리 · 최종 갱신: 2026-09-14
> **출처**: 저장소 이슈·PR의 실제 기록 (#번호로 출처 표기). 증상 → 원인 분석 → 해결 → 재발 방지 순.
> **작성 기준**: "문서와 이력의 일치"가 채점 기준이므로, 모든 항목은 실제 이슈·PR과 연결한다.

---

## 요약 — 발생 유형별 분포

| 유형 | 건수 | 대표 |
|---|---|---|
| 프론트/UI 결함 | 4 | #26 페이지 500, #29 Enter 미전송, #139 사이드바 흔들림, 대화 삭제 동기화 |
| 인증/세션 결함 | 3 | #33 stale 세션, #54 기본 시크릿, #55 수명·폐기 |
| 회귀(머지 사고) | 2 | #60 Enter/IME 가드 소멸, #110 admin form 유실 |
| AI/시간 예산 | 3 | #41 타임아웃 10초, #73 커넥션 점유, AI 페이로드 정책 무시 |
| 배포/플랫폼 제약 | 4 | #81 Railpack 시작 명령, #100 SMTP 차단, CD 레이스, 빌드 지문 |
| DB/스키마 마이그레이션 | 3 | #51 FK cascade, alembic adopt 연쇄 3건 |
| 운영/관측성 | 2 | ASGI 행 현상, 스키마 상태 은폐 |
| 테스트 신뢰성 | 1 | #98 초 경계 flake |
| 명세·문서 불일치 | 다수 | #46~#50, #48, #53 |

---

## TR-01. HTML 페이지 전면 500 — JSON API만 검사하던 테스트의 사각지대 (09-07)

**증상** (#26): `/`, `/login`, `/signup`, `/logs` 네 페이지가 전부 500. JSON API는 정상이라 기존 테스트가 잡지 못했다.

**원인 분석** (PR #24):
1. `app/routers/pages.py`의 템플릿 경로 계산 오류 — 파일이 `app/routers/` 아래라 `parent.parent`가 `app/`까지만 올라가 존재하지 않는 `app/templates`를 가리킴 (`TemplateNotFound`).
2. `login.html`·`logs.html`에 Jinja2에 없는 JS식 삼항 연산자(`{{ mode == 'signup' ? '회원가입' : '로그인' }}`) 8곳 (`TemplateSyntaxError`).

**해결**: 경로 계산 정정 + Jinja2 문법(`{% if %}`)으로 전면 치환, 4페이지 200 회귀 테스트(`test_pages.py`) 추가.

**교훈**: "HTML 4종 + API 전 경로"를 커버하는 페이지 테스트가 CI 게이트에 들어갔다. **렌더링 계층은 API 테스트로는 절대 검증되지 않는다**는 것을 초기에 배운 사례.

## TR-02. Enter 전송 미구현 → IME 가드 설계 → **머지 회귀**까지 이어진 3막 (09-07)

**1막 — 증상** (#29): 채팅 입력이 `<textarea>`라 Enter가 줄바꿈으로 먹히고 보내기 버튼만 동작. textarea는 암묵적 폼 제출이 없다는 사실을 이슈 본문에서 규명.

**해결** (PR #30): `keydown` 핸들러로 Enter=전송/Shift+Enter=줄바꿈 관례 구현. 이때 **한글 IME 조합 중 Enter(`event.isComposing`)는 전송하지 않는 가드**를 함께 설계 — 한국어 서비스에서 자모 확정 Enter가 전송으로 오인되는 것을 원천 차단.

**2막 — 회귀** (#60, 09-07): PR #43 머지 직후 develop에서 `grep 'Enter|isComposing'`이 0건 — `feature/chat-updates`가 **오래된 분기점 코드를 갖고 와** #30의 커밋을 덮어썼다. 3-way 병합이 파일 단위 수동 충돌 처리에서 보존해 주지 못한 유형. 함께 유입된 것: `innerHTML` 보간 패턴(서버 detail 문자열이 HTML이 되는 위험 구조). `ruff`/`pytest`는 통과 — **JS 테스트가 없어 CI가 못 잡는 회귀**였다.

**해결** (PR #61): Enter/IME 가드 복구, `errorText()` 정규화 후 `textContent` 전용 렌더링, `test_frontend_contract` 정적 계약 검사 도입, PR 체크리스트에 "머지 직전 develop 재병합 → 삭제된 라인 diff 확인" 추가.

**교훈**: ① 기능 추가는 항상 최신 develop 기반으로 rebase 후 진행, ② CI가 못 잡는 영역(JS)에는 최소한의 정적 계약 검사라도 둔다, ③ 회귀는 "보고 → 복구 PR이 바로 뒤따르는" 절차로 즉시 처리.

## TR-03. DB 재생성 후 stale 세션이 다른 계정으로 해석됨 — 조용한 권한 승격 (09-07)

**증상** (#33): DB를 재생성하고 id=1로 다른 계정을 만들면, 예전 쿠키(`user_id=1`)가 **새 id=1 계정으로 200 인증** — 타 계정 대화까지 열람. 에러 로그가 전혀 없는 조용한 오동작.

**원인 분석**:
- 세션 쿠키의 `user_id` 정수를 blind 신뢰.
- SQLite는 AUTOINCREMENT가 아니라 최대 id 기반 재할당 → DB 재생성·삭제 시 id 재사용.
- 세션 해석 경로가 `get_current_user`·`_session_user` 2곳에 중복돼 검증 누락 여지 존재.

**해결** (PR #34): 로그인 시 `user_id`+`email_fp`(이메일 지문) 동시 저장, 해석 시 대조 — 불일치면 세션 파기+401. 해석 로직 단일 함수 통합 + DB 재생성 시나리오 회귀 테스트.

**교훈**: "식별자만 믿는 설계"는 저장소가 id를 재발급하는 순간 무너진다. **세션은 발급 대상과 묶이는 보조 식별자를 반드시 함께 담는다.** 이 원칙은 이후 #54(쿠키 위조) 대응의 토대가 됐다 — 바인딩이 있어도 시크릿이 공개되면 무력하다는 연쇄 분석으로 이어짐.

## TR-04. AI 타임아웃 10초는 너무 짧다 — 기본값 변경과 예산 개념 정리 (09-07)

**증상** (#41): 응답이 조금만 길어져도 504+AI_TIMEOUT. 실사용 불능 수준.

**해결** (PR #42): 기본값 10→45초. 변경 범위를 사전 조사해 **코드 1곳(config)+문서/예시 3곳**으로 확정(테스트는 TimeoutException 주입 방식이라 무관, 프론트는 별도 타임아웃 없음 — 이슈 본문에 근거 명시).

**심화** (PR #43): 네이토 API 실연동 + `AI_TIMEOUT_SEC=0.001` 강제 유발로 504 생존·에러 매핑·`ai_call_fail` 로그를 실측 검증 (역할카드 06 완료 조건 충족).

**교훈**: 기본값 하나도 "영향 범위 조사 → 변경 목록 → 수용 기준"을 이슈에 적는 습관이 리뷰 비용을 줄인다. 타임아웃이 "연결+읽기+재시도 포함 전체 예산"이라는 정의(ADR-003)는 이때 정리됐다.

## TR-05. 보안 전수 감사 — 하루에 발견한 Critical 6건과 일괄 수습 (09-07 → 09-08)

스스로 공격자 관점에서 전수 점검해 이슈 #54~#59를 등록, 09-08에 PR #62·#76~#79로 일괄 수습했다.

### TR-05-1. 공개 기본 SESSION_SECRET으로 쿠키 위조 (Critical, #54)
- **재현**: 코드·.env.example의 기본 시크릿으로 서명된 위조 쿠키로 uid 1/2의 대화 열람·대리 발화 가능. #34의 바인딩 방어도 시크릿을 아는 앞에서는 무력.
- **해결** (PR #62): **부팅 게이트** — 운영(DEBUG=false)에서 공개 기본값·32자 미만이면 `RuntimeError` 기동 거부. 단 **개발은 깨뜨리지 않는다**(경고+임시 키) — 평가자가 .env.example 복사만으로 앱을 띄울 수 있어야 하기 때문. 회귀 테스트 4패턴(약한값+운영=거부 / 약한값+개발=대체 / 강한값=통과 / 위조쿠키=401).

### TR-05-2. 세션 쿠키 문제 3건 (#55 수명·Secure·폐기, #59 email 평문)
- 14일 만료+Secure 없음+서버 폐기 수단 부재 / 페이로드에 email 평문(base64는 암호화가 아님).
- **해결** (PR #78): Max-Age 24시간·환경변수화(1~168h), 운영 Secure, **iat 기반 계정별 세션 전면 폐기 테이블**(`session_revocations`, `scripts/revoke_sessions.py --email`). email은 지문으로 제거.

### TR-05-3. GET limit=-1 무한 조회 (#56)
- SQLite 음수 limit은 무제한 → 200건 상한 우회. **해결**(PR #62): 상한 검증·정규화 + 회귀 테스트.

### TR-05-4. 보안 헤더 전무·운영 /docs 노출·Origin 무검증 (#57, #75)
- **해결**: PR #62에서 nosniff·X-Frame-Options·Referrer-Policy 기본 헤더를 먼저 추가하고, PR #79가 CSP·교차 출처 상태 변경 요청 403(Origin 없는 curl/스모크는 통과)·운영 `DOCS_ENABLED=false`에서 /docs 404를 완성. CSP 적용을 위해 프론트의 인라인 JS를 addEventListener 방식으로 전환한 점이 화면 명세(D-15)와 연결.

### TR-05-5. 로그인 무차별 대입·이메일 열거 타이밍 (#72)
- **실측**: 존재 이메일+오답 ~282ms(bcrypt) vs 미존재 ~12ms — 응답 본문이 같아도 시간으로 회원 열거 가능.
- **해결** (PR #76): 이메일별 실패 잠금(5회/15분, 429+Retry-After) + **미존재 이메일에도 더미 bcrypt 수행**으로 타이밍 평탄화 + `login_fail`/`login_rate_limited` 이벤트(이메일 도메인만 기록).

**교훈**: ① 감사는 "기능이 동작한다"와 "막혀 있다"를 분리해서 본다 — 동작 테스트만으로는 보안 결함이 안 보인다. ② 실측 수치(282ms/12ms, Errno 101)를 이슈에 남겨야 해결의 검증 기준이 된다. ③ 여러 건을 발견해도 **완료 기준을 이슈별로 분리**해 두면 일괄 수습 후에도 검증이 개별적으로 가능하다.

## TR-06. FK cascade가 실제로는 없었다 — ERD 주장과 SQL 현실의 불일치 (09-07)

**증상** (#51): ERD가 주장하는 cascade 삭제가 ORM 경로에서만 성립. `PRAGMA foreign_keys` 미설정이라 SQL 레벨에서는 FK 자체가 미강제.

**해결** (PR #63): SQLite 연결 시 `PRAGMA foreign_keys=ON` 강제 + `ondelete=CASCADE` 정합 확인 + 시드 스크립트 경로 안내 정정(#52)과 `MAX_QUESTION_LENGTH` 실연결(#53, 데드 설정을 schemas·화면까지 전파)을 한 릴리스에 묶어 정정.

**교훈**: **문서(ERD)에 적힌 제약은 DB가 실제 강제하는지 테스트로 증명**해야 한다. "문서와 구현의 불일치"는 기능 부재만이 아니라 "문서만 존재하는 제약"에서도 생긴다.

## TR-07. Railway 배포 실패 "No start command detected" (09-09)

**증상** (#81): CD에서 Railway 빌드 FAILED — 배포 10376f75. 운영이 전면 중단된 상태.

**원인 분석**: Railway가 deprecated된 `railway.json`의 `startCommand`를 무시하고 신규 빌더(Railpack v0.39) 자동 감지로 전환 — `app/main.py` 구조는 감지 규칙에 없음.

**해결**: Nixpacks/Railpack 공통 인식 `Procfile`(`web: uvicorn app.main:app --host 0.0.0.0 --port $PORT`)을 루트 추가. `railway.json`은 헬스체크/재시작 정책 의도 문서로 유지. 긴급 머지는 CONTRIBUTING의 관리자 예외 절차(사전 승인·즉시 보호 복구)를 따랐고, 병합 후 develop→main 릴리스 PR + CD 재실행으로 배포 성공 검증 → 같은 날 운영 URL 게시(#83)까지 완료.

**교훈**: PaaS의 빌더 동작은 **언제든 바뀐다**. 시작 명령은 플랫폼 문서가 아닌 저장소의 파일(Procfile)이 단일 진실 원본이 되게 하고, 배포 관련 변경은 앱 코드와 분리해 원인 특정을 쉽게 했다.

## TR-08. Railway Free/Hobby SMTP 아웃바운드 차단 — 발송 경로 재설계 (09-10)

**증상** (#100): 운영에서 비밀번호 재설정 메일이 발송되지 않음(502). 로컬에서는 잘 동작했다.

**원인 격리 실측**: Railway Shell에서 `smtplib.SMTP_SSL("smtp.gmail.com", 465)` → `OSError: [Errno 101] Network is unreachable`. Gmail/Naver 무관, **플랜 자체가 SMTP 포트(25/465/587/2525)를 차단**(공식 문서·스태프 답변 확인) — 설정 값 문제가 아님을 먼저 증명.

**해결** (PR #101): **Resend HTTPS API(443) 경로 추가 + 우선순위 폴백**: `RESEND_API_KEY` 있으면 Resend → 없으면 기존 SMTP(로컬·Pro 대비) → 둘 다 없으면 기존 동작(개발=로그 출력, 운영=503). 실패 시 기존 502 경로 재사용(키·원문 노출 없이 상태코드만 로그). SMTP 테스트 10건 무수정 통과 + 신규 단위테스트 7건, 전체 205 passed. 운영 절차(가입→키 발급→Secrets→CD→1회 발송 테스트, 요청 상한 15분/3회 주의)까지 런북화.

**교훈**: ① 네트워크 오류는 **코드 밖(플랫폼 제약)과 안(버그)을 Shell 실측으로 먼저 분리**한다. ② 재설계는 폴백 구조로 기존 경로를 살려 회귀 안전망을 유지한다. ③ 발급→등록→검증 절차서가 없으면 해결 코드는 운영 성공과 다시 갈라진다.

## TR-09. CI만 실패하는 초 경계 flake (09-09)

**증상** (#98): 동일 커밋인데 push 실행에서만 `test_revoke_by_email_normalizes_case` 실패(`assert False is True`), PR 실행은 통과.

**원인 분석**: 폐기 전후로 `int(time.time())`을 두 번 호출하는 테스트 코드가 초 경계를 넘기면 `iat = epoch+1`이 되어 `iat <= epoch` 판정이 뒤집힌다. 앱 코드에는 #78 교훈의 유사 경합 대응이 이미 있었지만 **테스트가 같은 실수를 반복**했다.

**해결** (PR #99): 폐기 전 시각을 `iat`으로 사용하는 결정적 단언으로 수정, 동일 파일 유사 패턴 점검.

**교훈**: 시간 기반 테스트는 "실제 now를 두 번 부르지 않는다" — 기대값을 사전에 고정해야 경계에서 flake가 사라진다. flake는 원인 불명 채로 재실행하지 않고 반드시 원인 확정 문서를 남긴다.

## TR-10. 문서·이력 불일치 전수 감사 (09-07)

**증상**: 일괄 감사에서 다수 발견 — #46(기본 브랜치 main에 앱 코드 0건), #47(README 개인별 요약이 빈 플레이스홀더), #48(로그 이벤트 종수가 README 6종/CONTRIBUTING 8종/실제 11종 3중 불일치), #49(감사 수치 미갱신), #50(TODO 사실 오류 2건: max_age '미설정'은 실제 14일, Enter 전송 '미완료'는 이미 머지됨).

**해결** (PR #63·#64·#66 등): 사실 오류 정정 → develop→main 릴리스로 기본 브랜치 정합화 → "이후 문서 수치는 실측 명령(shortlog 등)과 함께 기록" 규칙화. 이후 감사 기록 갱신(#96)과 "stale 사실 정정"(배포 상태·세션 수명 등)을 상시 운영.

**교훈**: 문서 불일치는 악의가 아니라 **갱신 규칙 부재**에서 온다. "수치는 명령 재현과 함께, 상태 표기는 날짜와 함께"를 컨벤션으로 박제했고, TODO.md에는 "과거 기록을 새 코드의 승인/검증인 것처럼 바꾸지 않는다"는 원칙이 남았다.

## TR-11. 데모 모드를 실연결로 표시하는 오류 (09-07)

**증상** (#58): `AI_API_KEY`가 **빈 문자열**이면 /health·화면 배너가 '실연결'처럼 표시 — Fake 응답인데도 데모가 아닌 것처럼 보임.

**해결**: 키 유효성 판정을 "값 존재"로 엄밀히 하고 `ai_mode`(real/demo)를 /health로 공개, UI 배너·README·M3 증빙이 같은 근거를 공유. 실 AI 검증은 `scripts/ai_check.py --require-real`(키 없으면 종료 2, 성공으로 위장 안 함)로 별도 수행.

**교훈**: "동작한다"와 "진짜다"의 경계가 채점 신뢰성의 핵심 — **증빙의 등급(로컬/Fake/모의 HTTP/운영 실AI)을 문서가 항상 자기표시**해야 한다.

## TR-12. 프로덕션 행 — ASGI receive 래핑이 HTTP/2에서 응답 블로킹 (09-11)

**증상**: 운영에서 간헐적으로 응답이 멈추는 행(hang) 현상. 요청이 수 초~수십 초 동안 반환되지 않다가 타임아웃.

**원인 분석** (`b6052ed`):
- 바디 사이즈 가드 미들웨어가 Starlette ASGI `receive()`를 클로저로 감싸 런타임에 누적 바이트를 검사하려 함.
- HTTP/2 · 멀티웨이커 환경에서 이 래핑이 응답 파이프라인을 블로킹 — Content-Length 프리헤더 검사만으로도 충분한데 런타임 읽기 방어가 과도했다.
- 함께 발견된 문제(`baac01c`): `lifespan`의 `init_db()`가 DB 연결 실패 시 프로세스를 뻗게 함. 운영 DB 장애 시 앱 전체가 기동 불가.

**해결**:
1. receive 래핑 제거 — Content-Length 프리헤더 검사만 남김. 청크 전송 방어는 엣지/로드밸런서 레이어로 위임(HARDENING_BACKLOG B5).
2. `init_db()` 실패 시 프로세스가 뻗지 않고 에러만 로그로 남기고 기동(`/readyz`는 503).
3. Postgres 커넥트 타임아웃 3초로 제한 — DB 장애 시 `/readyz`가 수십 초 블로킹되지 않도록.
4. `readyz_db_failure` 이벤트를 EVENTS allowlist에 등록.

**교훈**: ① 미들웨어가 ASGI 수명주기를 감싸면 **프로토콜 레벨의 부작용(HTTP/2 행)**이 생길 수 있다 — 필요한 최소 검사만 남긴다. ② **DB 장애가 앱 기동 실패로 전파되면 안 된다** — 헬스체크(`/readyz`)로 격리하고, 프로세스는 항상 올라가 있어야 한다.

## TR-13. AI 클라이언트 3중 결함 — 페이로드 무시·이중 타임아웃·엔드포인트 정규화 (09-11)

**증상** (AUDIT_HARDENING.md 감사에서 발견):
1. `AI_MAX_TOKENS`·`AI_TEMPERATURE` 설정을 바꿔도 AI 응답에 반영되지 않음.
2. 전체 예산(`asyncio.timeout`)과 httpx timeout이 이중 적용돼 타임아웃이 예측 불가능.
3. `normalize_endpoint`가 기본 예시 URL(`/v1/chat/completions`)에 접미사를 중복 붙임.

**원인 분석** (`ce47388`):
- `build_payload()`가 `_attempts` 내부에서 호출되지 않고 외부에서 한 번만 실행 → 재시도 시 설정값이 무시됨.
- httpx 클라이언트에 `connect`/`write`/`pool`/`read` 세분 타임아웃 대신 단일 timeout을 써서 asyncio.timeout과 경합.
- `normalize_endpoint`가 이미 `/v1/chat/completions`를 포함한 URL에 또 붙이는 버그.

**해결**:
1. `_attempts`가 매 시도마다 `build_payload()`를 호출하도록 변경 + 회귀 테스트.
2. 타임아웃을 per-attempt 세분화(connect 5s / write 10s / pool 5s / read=잔여)로 정리.
3. `normalize_endpoint` 정규화 로직 수정 + 회귀 테스트.
4. `finish_reason=length` 응답에 말줄임 표식(…) — 조용한 응답 끊김 방지.
5. httpx `AsyncClient` limits 지정으로 keep-alive 연결 재사용.
6. 비밀번호 재설정 실패 로그가 `str(exc)` 원문을 남기던 부분 → 타입 이름만 기록(민감정보 정책).
7. SQLite/Postgres dialect 분기 — `SingletonThreadPool` vs `pool_size`/`max_overflow`.

**교훈**: **설정이 코드에 연결되지 않으면 "데드 설정"** — `.env`에 아무리 값을 바꿔도 조용히 무시된다. 설정→사용지 전체 경로를 통합 테스트로 고정해야 한다. 타임아웃은 "전체 예산 = per-attempt × 재시도 수" 공식이 성립하도록 설계한다.

## TR-14. CD 파이프라인 — 난독화 검증 레이스 + 빌드 지문 보장 (09-12)

**증상**: CD가 간헐적으로 "JS 난독화 배포 검증" 단계에서 실패 — 원본 JS가 배포된 것처럼 잘못 보고(허위 적신호).

**원인 분석** (`567c286`):
- `railway up`은 비동기 — 새 배포가 서빙되기 전에 반환.
- 검증 단계가 즉시 curl하면 아직 구버전을 서빙 중인 운영에서 원본 JS를 받음.
- "소스 주석 발견"으로 잘못 실패 처리.

**해결**:
1. 로컬 결정적 산출물 sha256과 배포 파일이 바이트 일치할 때까지 최대 30회(약 10분) 폴링 대기 후 검증.
2. 결정적 빌드(seed 고정)라서 산출물 해시 비교가 정확.

**추가 견고화** (`2cda803`, P1~P4):
- **P1** (중요): `/health`에 `build` 필드 추가(`settings.build_sha`). CD가 "health 200"이 아닌 **커밋 SHA 일치**를 대기 — 구버전 인스턴스에 health 통과하는 문제 해결.
- **P2**: Railway CLI 5.54.0 버전 고정 (major breaking change 방지).
- **P3**: `permissions: contents: read` (GITHUB_TOKEN 최소 권한).
- **P4**: `timeout-minutes` — ci/browser 30, gate 15, deploy 45 (기본 360분 방지).

**교훈**: ① "배포했다"와 "배포가 서빙 중이다"는 다르다 — **빌드 지문(SHA)이 운영에서 확인되어야 진짜 배포 완료**. ② 비동기 배포 플랫폼에서는 검증 전 **롤아웃 완료 폴링**이 필수. ③ CD는 "초록이면 성공"이 아니라 "새 버전이 서빙 중이면 성공"이어야 한다.

## TR-15. alembic 도입 후 레거시 DB 스키마 동기화 실패 3연발 (09-12 → 09-13)

**배경**: 09-12에 alembic을 도입(`ceed2b7`)했으나, 운영 DB는 그 이전 `create_all`로 만들어진 레거시 — `alembic_version` 테이블이 없었다.

### 1차: 레거시 DB 자동 인수 (PR #127, `9b2fc79`)

**증상**: `init_db`의 `upgrade head`가 alembic_version 없는 DB를 "아무것도 안 적용된" DB로 오인 → init 마이그레이션을 처음부터 재실행 → `table users already exists`로 항상 실패. lifespan이 오류를 로그로만 삼켜 프로세스는 기동했으나 스키마는 갱신되지 않음. 09-13 threads 스키마 배포에서 신규 API 500으로 드러남.

**해결**: alembic_version 없는 DB 자동 인수 — 스키마가 최신이면 head 스탬프, 아니면 루트 스탬프 후 upgrade head.

### 2차: 스키마 상태 은폐 문제 (PR #128, `6042400`)

**증상**: 1차 수정 후에도 `/health`는 200, `/readyz`는 `SELECT 1`만 봐서 스키마 미반영 인스턴스가 정상처럼 서빙.

**해결**: `schema_sync` 상태(ok/error:분류코드/revision)를 `/health`에 노출 + `/readyz`는 스키마 실패 시 503. `_classify_db_error`로 원문 노출 없이 분류 코드만 기록.

### 3차: 빈 alembic_version 잔여 (PR #129, `a652e8f`)

**증상**: 1차 adopt가 없는 구버전이 기동하면 upgrade가 alembic_version 테이블을 만든 뒤 root 재실행 실패 — SQLite DDL이 비트랜잭셔널이라 실패 후에도 **빈 테이블이 잔여**. 1차 adopt는 "테이블 부재"만 검사해 이 상태에서는 실행되지 않음 → 매 기동 무한 반복.

**해결**: `_has_version_row` 추가 — 테이블이 존재하되 0행이면 레거시로 판정, adopt 실행.

**교훈**: ① 마이그레이션 도구 도입은 **기존 DB를 어떻게 인수할 것인지**를 반드시 함께 설계해야 한다. ② "테이블 존재 여부"가 아니라 **"의미 있는 데이터가 있는지"**를 검사해야 한다. ③ 스키마 동기화 실패는 **헬스체크가 감지해야** 한다 — 로그에만 묻히면 운영이 조용히 썩는다. ④ SQLite DDL은 비트랜잭셔널이라 실패 후 잔여물이 남을 수 있다.

## TR-16. 병렬 머지로 유실된 관리자 form 입력 (09-11)

**증상**: 관리자 로그 조회 페이지에서 폼 제출이 깨짐 — `admin-logs.js`가 참조하는 `#admin-reason` 요소가 HTML에 없음.

**원인 분석** (`c59e7c7`):
- PR #110에서 병렬 편집(페이지네이션 보존)만 반영되고, form 입력 추가가 누락됨.
- 여러 PR이 같은 파일을 동시에 수정할 때 3-way 병합이 "행 단위"로는 성공하지만 **논리적 완전성**은 보장하지 못함.

**해결**: 입력 요소 복원 + 렌더 HTML에 요소 존재를 단정하는 계약 테스트(`test_frontend_contract`)로 재발 방지.

**교훈**: 병렬 PR이 같은 템플릿 파일을 수정하면 **행 충돌 없이 논리적 누락**이 생긴다. JS가 참조하는 DOM 요소는 계약 테스트로 "존재 보장"을 자동화해야 한다.

## TR-17. 배포 후 UI 변경 미반영 + 테마 피드백 부재 (09-13)

### 정적 자산 캐시 버스팅 (`a2dcfd3`)

**증상**: 배포 후 브라우저에 캐시된 옛 CSS/JS가 남아 "변경사항이 안 보인다"는 사용자 보고.

**해결**: Jinja 전역 `asset_v = build_sha[:7]`을 템플릿 10곳(CSS/JS 9 + favicon)에 `?v={{ asset_v }}`로 주입. 계약 테스트 `test_static_assets_are_versioned` 추가.

### 테마 토글 피드백 부재 (`39fdf15`)

**증상**: 테마 '시스템' 모드로 토글해도 화면이 안 바뀌는 것처럼 보임.

**원인 분석**: 기능은 정상(OS 색 실시간 추적)이나, OS 색이 현재 표시 색과 같으면 외형 변화 0 — 아이콘(◐/☀/☾)만으로는 '바뀌었다'는 걸 인지 불가.

**해결**: 버튼에 모드 이름 표시(◐ 시스템 / ☀ 라이트 / ☾ 다크) — 클릭마다 라벨이 바뀌어 명확한 피드백.

**교훈**: ① 정적 자산은 **버전 지문 없이 배포하면 캐시에 묻힌다** — CD의 빌드 SHA를 URL에 태우는 것이 가장 단순한 해결. ② UI 피드백은 "기능이 동작하는가"가 아니라 **"사용자가 동작을 인지하는가"** 기준으로 설계한다.

## TR-18. 스레드 기능 도입 후 클라이언트 동기화 결함 + 사이드바 UX (09-14)

### 대화 삭제·새 채팅 동기화 (`e98f132`)

**증상**: 현재 대화를 삭제하면 빈 채팅창에 입력한 메시지가 서버 컨텍스트와 어긋나 "DB 꼬임" 발생. 마지막 대화 삭제 후 첫 전송에서 목록만 비어 있는 불일치.

**원인 분석**: 목록 갱신보다 채팅창 동기화가 먼저 일어나거나, 삭제 후 남은 기본 대화가 서버에서 다른 스레드를 가리키는 상태.

**해결**: 목록 갱신(삭제된 대화 소멸)이 **먼저** 이뤄진 뒤 채팅창이 남은 대花的이력으로 동기화. 채팅 전송은 성공·실패 모두 목록 항상 동기화. 기록·관리자에서 새 채팅은 `?thread=<새 id>`로 이동.

### 사이드바 반응형 전환 흔들림 (#139, `7f139f5`)

**증상**: 브라우저 창을 768px 근처에서 리사이즈하면 사이드바가 반복적으로 나타나고 사라지며 흔들림. 애니메이션이 `.22s ease`로 급격.

**해결**: 단일 경계(768px)를 이중 경계(축소 700px / 확대 900px)로 변경 — 두 경계 사이는 이전 상태 유지(히스테리시스). CSS transition을 `.5s cubic-bezier(.22,1,.36,1)`로 변경 — 빠르게 시작해 로그 함수처럼 점점 느려지며 수렴. 데스크톱 접기를 `display:none` → `width:0` 트랜지션으로 부드러운 수축/확장.

**교훈**: ① 목록↔채팅창 동기화는 **순서가 핵심** — 서버 상태를 먼저 반영한 뒤 UI를 갱신해야 꼬임이 없다. ② 반응형 경계는 **1개면 흔들린다** — 히스테리시스로 데드밴드를 두는 것이 산업 표준(thermostat pattern). ③ 애니메이션 easing은 "선형"이 아니라 "자연스러운 감쇠"가 사용자 경험에 훨씬 좋다.

## 부록 — 재발 방지 규칙으로 전환된 교훈

| 교훈 | 전환된 규칙/장치 |
|---|---|
| 렌더링은 API 테스트로 못 잡는다 (TR-01) | 페이지 4종 회귀 테스트 CI 게이트 |
| 오래된 분기에서의 머지는 회귀를 낳는다 (TR-02) | 머지 직전 develop 재병합 + 삭제 라인 diff 확인 (PR 체크리스트), test_frontend_contract |
| 식별자 blind 신뢰 금지 (TR-03) | 세션-계정 바인딩 + iat 폐기 + 시크릿 게이트 3중 구조 |
| 시간 테스트는 두 번 now 금지 (TR-09) | epoch 정수 원칙(D-23) + 결정적 단언 규칙 |
| 플랫폼 제약은 실측 선확인 (TR-07/08) | "배포 전 실측" M1 회고 정식 교훈, 폴백 설계 원칙 |
| 문서 수치는 재현 명령과 함께 (TR-10) | commit-audit.md 집계 규칙, 감사 기록 상시 갱신 |
| 데모와 실연결 혼동 금지 (TR-11) | ai_mode 공개 + 증빙 등급 표기 원칙 (README, M3) |
| ASGI 래핑은 프로토콜 부작용 유발 (TR-12) | 미들웨어는 최소 검사만 + DB 장애 격리(`/readyz` 503) |
| 데드 설정은 통합 테스트로 잡는다 (TR-13) | 설정→사용지 전체 경로 서브프로세스 테스트, 타임아웃 세분화 |
| 배포 완료를 SHA로 증명한다 (TR-14) | `/health.build` 지문 + CD 롤아웃 폴링, CLI 버전 고정 |
| DB 마이그레이션 인수는 데이터 기준으로 (TR-15) | 빈 테이블 잔여 검사, `/health.schema` + `/readyz` 503 |
| 병렬 머지의 논리적 누락은 계약 테스트로 (TR-16) | DOM 요소 존재 단정 테스트(test_frontend_contract) |
| 정적 자산은 버전 지문과 함께 배포 (TR-17) | `?v={{ asset_v }}` 빌드 SHA 주입, 계약 테스트 |
| 반응형 경계 1개 = 흔들림 (TR-18) | 히스테리시스 이중 경계(thermostat pattern), 로그 easing |
| 목록↔UI 동기화는 순서가 핵심 (TR-18) | 서버 상태 선반영 → UI 후갱신 원칙 |
