# 커밋 / PR 감사 (#2)

> 과제 요구사항: **"팀원별 유의미한 커밋이 10회 이상 존재해야 한다"** (PDF 7절 · 6절 협업 규칙에서 재차 명시)
> 담당: 이종석(#2) · 점검 주기: 매주 월요일 · 기준 브랜치: `develop`

## 재현 명령

```bash
git fetch origin
git shortlog -sne origin/develop          # 팀원별 커밋 수
gh pr list --state merged --limit 50      # 머지된 PR
```

---

## 1주차 — 2026-09-07 (월)

### 팀원별 커밋 수 (`origin/develop` 기준)

| 팀원 | 작업 커밋 | (머지커밋 포함) | 머지된 PR | 목표 10회까지 | 상태 |
|---|---:|---:|---:|---:|---|
| giyeop-cody | **30** | 35 | 8 | 달성 | ✅ |
| Im-Jongseok | **11** | 16 | 3 | 달성 | ✅ |
| loader1017 | **0** | 1 | 0 | **10** | ❌ |

집계 시 주의할 점 2가지 — 다음 주 감사에서도 동일하게 적용한다.

> **① 머지 커밋은 제외한다.** `git shortlog -sne`는 머지 커밋까지 세므로 실제보다 부풀려진다.
> 본 표의 "작업 커밋"은 `git shortlog -sne --no-merges` 기준이다.
> **loader1017의 1건은 PR #22를 머지한 커밋**이며, 직접 작성한 커밋은 아직 0건이다.
> 아래 "유의미한 커밋 기준"에 비춰 집계 대상이 아니다.
>
> **② giyeop-cody는 author 이메일이 2개다.** 계정 이메일과 GitHub noreply로 나뉘어
> `shortlog`에 23 / 7로 분리 표시된다. 합산 30건.

머지된 PR 11건 — giyeop-cody 8건(#15 스켈레톤, #19 세션 방어, #20 운영 정책, #30 Enter 전송,
#32 문서 경로 정정, #34 세션 바인딩, #39 Swagger 보강, #42 타임아웃 45초),
Im-Jongseok 3건(#21 ERD·SQL 수정, #22 문맥 시연, #24 HTML 500 수정).

### 판정: ⚠️ 주의

**3명 중 1명이 0커밋.** 1주차 최초 점검 시점에는 2명이 0커밋이었으나,
Im-Jongseok이 PR #21·#22·#24로 11커밋을 반영해 목표를 달성했다.
loader1017은 PR 머지는 수행했으나 **직접 작성한 커밋이 아직 없다.**
PDF는 "문서에 팀 역할/개인별 작업 요약이 포함되어야 하며, **Git 이력과 크게 모순되지 않아야 한다**"고
요구하므로, 역할 분담 문서를 아무리 잘 써도 커밋이 없으면 모순으로 감점된다.

### 원인

초기 스켈레톤(#15)을 PM이 단독으로 올려 코드 골격이 한 번에 완성되면서,
나머지 팀원의 작업이 "이미 있는 코드 다듬기"로 바뀌었고 착수가 늦어졌다.

### 남은 일정과 필요 속도

M3 제출 2026-09-28까지 **3주**. 미달자(loader1017)는 **주당 3~4커밋**이면 달성한다.

| 주차 | 기간 | 인당 목표 | 누적 |
|---|---|---:|---:|
| 2주차 | 09/08 ~ 09/14 (M1) | 4 | 4 |
| 3주차 | 09/15 ~ 09/21 (M2) | 3 | 7 |
| 4주차 | 09/22 ~ 09/28 (M3) | 3 | 10 |

### 조치

1. **이종석**: PR #21·#22·#24 머지 완료로 작업 커밋 11건 — 목표 달성.
   본 문서 브랜치가 머지되면 추가 반영된다.
2. **loader1017**: 이슈 #18에서 지정된 첫 PR(네이토 키 검증 #6 / Enter 전송·마크다운 #9) 착수 요청.
3. 하루 작업을 **2~3개 논리 단위로 쪼개 커밋** (`CONTRIBUTING.md` 규칙) — 한 방 커밋 금지.
4. **Squash 금지 유지** — 머지 시 커밋이 1개로 합쳐지면 위 수치가 그대로 소멸한다.
   저장소 설정의 `allow_squash_merge=true` 해제를 @giyeop-cody 에게 요청 (`docs/git-rules.md` 3절).

### "유의미한 커밋" 기준

수치만 채우는 커밋은 감점 요인이다. 아래를 만족해야 1건으로 집계한다.

- 커밋 메시지가 규약 형식(`type(scope): 제목 (#N)`)을 따르고 무엇을 왜 바꿨는지 드러날 것
- 하나의 논리 단위일 것 (기능 + 오타 수정 섞지 않기)
- 공백/포맷만 바꾸거나 되돌리기 위한 커밋은 제외

---

## 2주차 — (2026-09-14 기록 예정)

---

## 2주차 — 2026-09-08 (화, develop `9acffce` 기준)

1주차 표는 그 사이에 머지된 PR(#36·#37·#39·#40·#42·#43)을 반영하지 못해 어긋나 있었다 (#49).
이번 표는 아래 명령으로 계산한 값 그대로다.

```bash
git log --no-merges --format='%ae' origin/develop | sort | uniq -c   # 이메일 단위
# giyeop-cody = cody.giyeop@gmail.com(24) + noreply(14) = 38
```

| 팀원 | 작업 커밋 | 머지한 PR | 목표 10회까지 | 상태 |
|---|---:|---:|---:|---|
| giyeop-cody | **38** | 13 | — | ✅ |
| Im-Jongseok | **18** | 4 | — | ✅ |
| loader1017 | **1** | 1 (#43) | **9** | ❌ |
| ygyg | 0 | 0 | 10 | ❌ (미참여) |

- develop 총 커밋 79 / 머지된 PR 18건 (`merged_at` 기준). 기준 커밋 `7501fd4`(PR #61 머지 결과).
- loader1017: 1주차 0건 → PR #43(#6) 머지로 1건. **전 브랜치(25개)를 search 해도 추가 기여가 1건뿐**이라
  남은 9건은 새로 만들어야 한다. 담당 카드 09·10·11 + 이슈 #18을 이번 주 착수 목표으로 지정 (#45).
- PR #43 머지로 **#29 Enter 전송·IME 가드가 develop에서 소멸**하는 회귀가 발생했다 (JS 테스트가 없어 CI가 못 잡았다)
  → 복구 PR **#61 머지 완료**(`7501fd4`), 재발 방지 항목을 PR 템플릿·CONTRIBUTING 4절에 추가 (#60).

## 2주차 마감 — 2026-09-08 07:19 (KST), develop `dc3a080` 기준

위 2주차 표(`9acffce`) 이후 머지 6건(#62·#63·#64·#66·#67·#68)을 반영한 **현재 값**이다. 이 표 역시 문서가 아니라 Git에서 계산하며, 재현 명령:

```bash
git shortlog -sne --no-merges origin/develop          # 정체별(이메일 단위)
git log --no-merges --format='%ae' origin/develop | sort | uniq -c
#   giyeop-cody = cody.giyeop@gmail.com(33) + noreply(14) = 47
```

| 팀원 | 작업 커밋(비머지) | 머지한 PR | 목표 10회까지 | 상태 |
|---|---:|---:|---:|---|
| giyeop-cody | **47** | 19 | — | ✅ |
| Im-Jongseok | **18** | 4 | — | ✅ |
| loader1017 (Sungeun `dohc2069@naver.com`) | **1** | 1 (#43) | **9** | ❌ |
| ygyg | 0 | 0 | 10 | ❌ (미참여) |
| `Arena Agent <agent@arena.local>` | 1 | — | — | ⚠️ 아래 기록 참조 |

- develop 총 커밋 **98**(비머지 67) / GitHub에서 `merged_at != null`로 센 머지 PR **24건**(develop 23 + main 1, #66).
- 직전 표 대비 증가분은 전부 에이전트 세션의 정적 작업(#54–#59 보안, #51–#53 위생, #47–#50 문서)이며,
  **팀원 개인 기여 요구는 이 수치로 채워지지 않는다** — loader1017·ygyg의 커밋은 그대로다 (#45).
- `main`은 #66 머지로 앱 코드를 갖게 되었다 (#46). 방향 검증: `git rev-list --count origin/main ^origin/develop` = **0** → `main`은 develop의 완전한 부분집합이라 되돌릴 커밋이 없고,
  develop이 #67·#68로 **4커밋 앞서 있다**(트리 차이 = README.md 1파일 +7/−1). 다음 릴리스 PR로 흡수하면 두 브랜치가 내용까지 동일해진다.

### 비팀원 작성 커밋 1건 (투명성 기록)

어시스턴트 세션의 로컬 git identity가 남은 커밋이 2건 있다. 둘 다 **콘텐츠 변경 0건**이고
코드 작성자 귀속에는 영향이 없으나, `--no-merges` 집계·PR 화면에 보이므로 여기에 기록해 둔다.

| 커밋 | 형태 | 유입 경로 | 내용 |
|---|---|---|---|
| `d374bbb` `Merge branch 'develop' into docs/#1-todo-sync` | 머지(부모 2) | PR #40 | develop → 브랜치 병합 충돌 해결. `--no-merges` 집계에서 제외됨 |
| `2e8448e` `placeholder` | **비머지·빈 커밋** | PR #61 | 어시스턴트 PR 생성 스크립트가 `--allow-empty`로 만든 더미 커밋. **diff 0건** |

조치: `placeholder` 커밋은 내용 자체가 없어 되돌릴 게 없다. develop은 룰셋(`non_fast_forward`)으로 보호돼
force-push 없이 제거할 수 없으므로, **다음 히스토리 정리(D10 이후) 때 `git filter-repo`로 함께 제거**하는 편이 깔끔하다.
앞으로 어시스턴트 작업은 ① 팀원 계정으로 커밋하고 ② 빈 커밋을 만들지 않으며 ③ 이 문서에 각주한다.
