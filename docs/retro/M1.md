# M1 회고 (2026-09-10 작성 · 스프린트 종료 시 갱신)

## 진행
- 실 AI 전환 완료 — GitHub Secrets(AI_API_KEY/AI_BASE_URL/AI_MODEL) → CD → 운영 `/health` `ai_mode=real` 실측(2026-09-10 17:41 KST CD success)
- 이메일 발급 경로 — Railway Free/Hobby의 SMTP 아웃바운드 차단 확인(Shell 실측 `Errno 101`) → Resend HTTPS API로 전환(PR #101, merge `a6955f4`, 이슈 #100 자동 종료)
- 운영 로그 — 구조화 이벤트 실측 확보: `auth_password_reset_rate_limited`(요청 트레이스 3줄), `admin_user_deleted`(테스트 계정 23건 정리), `ai_call_success`(채팅)
- 관리자 부트스트랩 — `manage_admin.py grant` + 재로그인 + `/admin/logs` 접근 확인

## 팀원 스탠드업 요약(각 1줄 — 본인이 직접 채운다)
- giyeop:
- Im-Jongseok:
- ygyg:
- loader1017:

## 마일스톤 근거
- 커밋 수(전원 ≥10): giyeop 36 / Im-Jongseok 32 / ygyg 14 / loader1017 11 (develop 기준, 2026-09-09 실측 — 제출 시 재측정)
- 테스트: pytest 205 passed · ruff/black/isort 통과(PR #101 CI 녹색)
- 운영: `/health` 200 real · CD 전 구간(게이트→동기화→배포→헬스→E2E 스모크) 성공
- 실 AI: 운영 채팅 실응답 2회 실측(1.6~2.0초)

## 리스크 / 조치
- Railway 플랜 제약: SMTP 아웃바운드 차단 → Resend(443) 우회로 설계 변경. 교훈: 플랫폼 제약은 배포 전 실측으로 확인
- 설정 이중화 위험: Railway Variables 직접 수정은 다음 CD 때 덮어써짐 → **GitHub Secrets를 유일한 원본으로** 운영
- 승인 대기 병목: ruleset 필수 승인 1명 요건 — 리뷰 가능한 멤버 확보 또는 운영 판단 우회 시 반드시 기록 남김(PR #101 코멘트 참조)
- 재설정 요청 상한(15분/3회): 발송 실패한 시도도 소진 → 테스트는 1회성으로 계획
